#include<iostream>
#include<cstring>
using namespace std;
int main(){
    char str[100];
    cin>>str;
    int len=strlen(str);
    for(int i=0,j=len-1;i<=j;i++,j--){
        swap(str[i],str[j]);
    }
    cout<<str;
}
