#include<iostream>
using namespace std;
int main(){
    string a,b;
    cin>>a;
    cin>>b;
    if(a.size()!=b.size()){
        cout<<"no";
        return 0;
    }
    string temp=a+a;
    if(temp.find(b)!=string::npos){
        cout<<"yes";
    }
    else cout<<"no";
}
