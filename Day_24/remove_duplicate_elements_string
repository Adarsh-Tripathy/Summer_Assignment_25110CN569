#include<iostream>
using namespace std;
int main(){
    string s;
    getline(cin,s);
    string word="";
    for(auto it:s){
        if(word.find(it)==string::npos){
            word+=it;
        }
    }
    cout<<word;
}
