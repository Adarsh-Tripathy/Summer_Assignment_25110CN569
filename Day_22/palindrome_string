#include<iostream>
using namespace std;
int main(){
    string str;
    cin>>str;
    string a=str;
    for(int i=0,j=str.size()-1;i<=j;i++,j--){
        swap(str[i],str[j]);
    }
    if(a==str){
        cout<<"palindrome string ";
    }
    else cout<<"not palindrome ";
}
