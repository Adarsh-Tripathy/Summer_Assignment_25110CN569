#include<iostream>
#include<algorithm>
#include<cstring>
using namespace std;
int main(){
    char str[100];
    cin>>str;
    char a[100];
    strcpy(a,str);
    reverse(a,a+strlen(str));
    if(strcmp(a,str)==0){
        cout<<"palindrome string";
    }
    else cout<<"not palindrome string";
}
