#include<iostream>
using namespace std;
int main(){
    string str;
    getline(cin,str);
    string a;
    for(int i=0;i<str.size();i++){
        if(str[i]!=' '){
            a+=str[i];
        }
    }
    cout<<a;
}
