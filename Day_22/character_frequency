#include<iostream>
using namespace std;
int main(){
    string str;
    getline(cin,str);
    int count=0;
    char k;
    cin>>k;
    for(int i=0;i<str.size();i++){
        if(str[i]==k){
            count++;
        }
    }
    cout<<count;
}
