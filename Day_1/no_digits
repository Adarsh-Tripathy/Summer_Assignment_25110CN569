#include<bits/stdc++.h>
using namespace std;

int main(){
  int n;
  cin>>n;
  n=abs(n);
  int digits=0;
  if(n>0){
    while(n>0){
      n=n/10;
      digits++;
    }
    cout<<digits;
  }
  else cout<<1;
}
