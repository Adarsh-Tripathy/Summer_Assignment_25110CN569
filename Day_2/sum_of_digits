#include<bits/stdc++.h>
using namespace std;
int main(){
  int n;
  cin>>n;
  int a;
  int sum=0;
  n=abs(n);
  if(n==0){
    cout<<0;
    return 0;
  }
  else{
    while(n>0){
      a=n%10;
      sum=sum+a;
      n=n/10;
    }
  }
  cout<<sum;
}
